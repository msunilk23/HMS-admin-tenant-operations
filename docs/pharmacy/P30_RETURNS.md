# P30 — Patient + Supplier Returns
Patient return references original dispense and batch; restock only policy-eligible items. Others quarantine/dispose. Supplier returns reference supplier/GRN/batch and support damaged, wrong supply, short expiry, recall and quality issues. Every accepted stock movement creates ledger entries. Approval-gate every task.
